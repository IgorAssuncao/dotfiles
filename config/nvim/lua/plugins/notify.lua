-- Plugin that allows notifications views like
-- any other OS notification program.

return {
    -- "rcarriga/nvim-notify",
    -- -- event = "Lazy",
    -- config = function()
    --     require("notify").setup({
    --         background_colour = "#000000",
    --         render            = "wrapped-default"
    --     })
    -- end
}
