-- Auto install LSP servers
return {
    "mason-org/mason.nvim",
    opts = {},
    -- config = function()
    --     require("mason").setup({
    --         log_level = vim.log.levels.INFO
    --     })
    -- end
}
